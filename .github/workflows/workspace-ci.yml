name: Workspace CI

on:
  pull_request:
  push:
    branches:
      - main
      - "codex/**"

permissions:
  contents: read

jobs:
  governance:
    runs-on: windows-latest
    timeout-minutes: 20

    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate JSON documents
        run: |
          Get-Content -Raw .\workspace.json | ConvertFrom-Json | Out-Null
          Get-Content -Raw .\LOOM.code-workspace | ConvertFrom-Json | Out-Null
          Get-ChildItem .\packages\contracts -Recurse -Filter *.json | ForEach-Object {
            Get-Content -LiteralPath $_.FullName -Raw -Encoding UTF8 | ConvertFrom-Json | Out-Null
          }

      - name: Install contract validation dependency
        run: python -m pip install --disable-pip-version-check --no-cache-dir jsonschema==4.25.1

      - name: Validate contract schemas and fixtures
        run: python -B .\packages\contracts\validate_contracts.py

      - name: Parse PowerShell scripts
        run: |
          $failed = $false
          Get-ChildItem .\scripts\*.ps1 | ForEach-Object {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile($_.FullName, [ref]$tokens, [ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              $failed = $true
              $errors | ForEach-Object { Write-Error "$($_.Extent.File):$($_.Extent.StartLineNumber) $($_.Message)" }
            }
          }
          if ($failed) { exit 1 }

      - name: Verify monorepo structure
        run: .\scripts\test-workspace.ps1

      - name: Reject submodules and nested Git repositories
        run: |
          if (Test-Path .\.gitmodules) {
            throw ".gitmodules must not exist in the monorepo."
          }

          $gitlinks = @(git ls-files -s | Where-Object { $_ -match '^160000\s' })
          if ($gitlinks.Count -gt 0) {
            $gitlinks | ForEach-Object { Write-Error "Gitlink is not allowed: $_" }
            exit 1
          }

          $nested = @(Get-ChildItem .\apps -Force -Recurse -Directory -Filter .git -ErrorAction SilentlyContinue)
          if ($nested.Count -gt 0) {
            $nested | ForEach-Object { Write-Error "Nested .git is not allowed: $($_.FullName)" }
            exit 1
          }

      - name: Verify default-branch push guard
        run: |
          $gitExe = (Get-Command git).Source
          $gitRoot = Split-Path (Split-Path $gitExe -Parent) -Parent
          $sh = Join-Path $gitRoot 'bin\sh.exe'
          $env:LOOM_ALLOW_MAIN_PUSH = '0'
          $ErrorActionPreference = 'Continue'
          'refs/heads/main 111 refs/heads/main 222' | & $sh ./.githooks/pre-push origin https://example.invalid/repo.git 2>&1 | Out-Null
          $blockedExit = $LASTEXITCODE
          $ErrorActionPreference = 'Stop'
          if ($blockedExit -eq 0) { throw 'pre-push hook did not block main' }
          $env:LOOM_ALLOW_MAIN_PUSH = '1'
          'refs/heads/main 111 refs/heads/main 222' | & $sh ./.githooks/pre-push origin https://example.invalid/repo.git
          if ($LASTEXITCODE -ne 0) { throw 'approved emergency bypass failed' }

      - name: Reject sensitive or generated filenames
        run: |
          $pattern = '(?i)(^|/)(\.env($|\.)|local\.properties$|.*\.(jks|keystore|p12|pfx|pem|key|db|sqlite|apk|aab|log)$|credentials.*\.json$|.*token.*\.txt$|.*授权码.*\.xlsx$)'
          $blocked = @(git ls-files | Where-Object { $_ -match $pattern })
          if ($blocked.Count -gt 0) {
            $blocked | ForEach-Object { Write-Error "Blocked tracked path: $_" }
            exit 1
          }

      - name: Check whitespace
        run: git show --check --format= HEAD
