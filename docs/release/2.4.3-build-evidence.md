# 麓鸣 2.4.3 / LumiAgent 6.64 本地候选证据

> 构建日期：2026-08-01
>
> 产品源码提交：`1fdbcef6567e2367f44de63027ccb5b3140b641d`
>
> 分支：`codex/account-entitlement-matrix-20260729`
>
> 范围：本地测试候选；未推更新通道、未创建正式 Release、未对外发布

## 1. 产物

候选目录：

`D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.3-1fdbcef6567e`

| 产物 | 字节 | SHA256 | 版本/兼容性 | 签名状态 |
| --- | ---: | --- | --- | --- |
| `麓鸣_2.4.3_x64-setup.exe` | 266,522,822 | `FD592CC96D20D14A73BE6399ADD39C9849B3E4EF4B72A85106445B5D01F13126` | File/Product `2.4.3`，ProductName/Description `麓鸣` | Authenticode `NotSigned`，仅本地测试 |
| `AgentPhone_v6.64-stability_20260801_202030.apk` | 52,091,108 | `37BAF09776E74ECC133491E213B30BCAF50D7D919C473F75FD5741AF8D2098BE` | versionCode `933`，versionName `6.64-stability`，minSdk `28` | APK v2 调试签名 |
| `AgentPhone_v6.64-stability-android7_20260801_202116.apk` | 19,645,761 | `B0404CDA57AEF895F2D7C248AFE4ECA2406BC8BE1B7297E9D3711A18D198EB84` | versionCode `933`，versionName `6.64-stability-android7`，minSdk `24` | APK v2 调试签名 |

两份调试 APK 的证书 SHA-256 均为：

`F6194A531BDD82F020080D21567E88864A1D8AC4BC27904FDF5DF46151426B6D`

该证书只用于本地调试候选，不等同于正式发布证书。真实 `assembleRelease` 继续要求独立配置 `KEYSTORE_FILE`、`KEYSTORE_PASSWORD`、`KEY_ALIAS` 和 `KEY_PASSWORD`，缺失时失败关闭。

## 2. 自动化与构建结果

| 层 | 结果 |
| --- | --- |
| Python 全量 | `2063 passed`，`367 subtests passed` |
| Frontend 平台合同 | `224/224` passed；2.4.3 版本切换后再次完整通过 |
| Node/CLI 合同 | `100/100` passed；2.4.3 版本切换后再次完整通过 |
| Playwright E2E | `210 passed`，`6 skipped`，`0 failed`；覆盖 Edge 的 960×640、1200×800、1440×900 三种视口 |
| Android default JVM | `663/663`，0 failed/errors/skipped |
| Android 7 JVM | `663/663`，0 failed/errors/skipped |
| Android API 36 instrumentation | 精确候选 APK 冷启动成功、无应用错误，`6/6` passed |
| Android API 24 instrumentation | 精确候选 APK 冷启动成功、无应用错误，`6/6` passed |
| Rust | `cargo check --locked` passed；`cargo test --locked` 为 `30/30` passed |
| Production frontend | Vite `11571` modules transformed，构建通过 |
| Protected release | Python `92` compiled + `6` loaders；scripts `23` copied + `25` obfuscated；验证通过 |
| Desktop NSIS | x64 bundle 构建通过；隔离目录静默安装/运行时/卸载冒烟通过 |

NSIS 冒烟实际确认：

- 使用随包 FastAPI、Python 与 Node runtime；
- 受保护路由模块来自 `.pyc`，未被旧源码覆盖；
- `loom-native` profile 可读，原生 Agent 能力目录为 `82` 项；
- 中文默认策略存在；
- 未授权时 license 公共接口返回 `200`，矩阵与获客接口均返回 `403`；
- 静默卸载完成并清除隔离安装目录。

本轮冒烟首次发现探针直接创建账号作用域 AgentService 后调用 bootstrap，会被正确的账号隔离门禁拒绝。发布脚本已改为显式不可执行的本地 `PackagedProbeRuntime`，不伪造账号、不放宽生产权限；对应合同测试 `7/7` 通过后，2.4.3 精确安装包冒烟通过。

## 3. 虚拟机验证

使用本机官方 Android Emulator + WHPX，逐台启动现有隔离 AVD；验证结束后 ADB 无遗留设备：

| AVD | 精确候选 | 结果 |
| --- | --- | --- |
| `LumiAgent_API36_242` / API 36 | default APK SHA256 `37BAF097…098BE` | 安装成功；`933 / 6.64-stability / minSdk 28 / targetSdk 36`；冷启动进入引导页；无应用错误；配对/热点/LAN instrumentation `6/6` |
| `LumiAgent_API24_242` / API 24 | android7 APK SHA256 `B0404CDA…8EB84` | 安装成功；`933 / 6.64-stability-android7 / minSdk 24 / targetSdk 36`；冷启动进入主页；无应用错误；配对/热点/LAN instrumentation `6/6` |

虚拟机日志目录：

`D:\AndroidAVD\evidence\lumiagent-2.4.3`

虚拟机不能替代物理手机热点、真实 USB 枚举与拔插、OEM MediaProjection、Shizuku Binder、温升/功耗、多设备长稳和真实弱网验收。

## 4. PRoot/Linux 与 Shizuku 的真实边界

- 对两份精确 APK 执行 ZIP 条目扫描，`openminis / proot / rootfs / alpine / debian / ubuntu / busybox / linux-runtime` 命中数均为 `0`。
- 因此 2.4.3 **没有把手机小 Linux 放进 APK**。当前只有 clean-room Schema、workspace 隔离、资源预算、Native-first 路由、回退策略、基准工具和供应链门禁。
- Shizuku 只包含官方兼容 API/Provider、能力探测、用户本地授权与类型化策略；未内嵌 Shizuku、`rish`、Root/Sui shell，也没有向模型或网络 API 暴露任意命令执行。
- 在 companion/rootfs 的来源、GPL 义务、SBOM、签名、回滚和实体机性能证据齐全前，不允许宣称 PRoot 已交付或已提速。

## 5. 尚未通过的正式发布门禁

1. 尚无实体机矩阵，未完成真实 Android 7/10/14+、热点/Wi-Fi/USB、USB+Wi-Fi、2 台并发、10 台两小时 soak、30 分钟视频与 20 次拔插。
2. 无正式 Android release keystore；当前只能交付调试签名 APK。
3. 无 Windows 商业代码签名证书；NSIS 候选为 `NotSigned`。
4. Shizuku 的 ADB/无线调试、binder 中断、撤权与重启恢复尚缺实体机证据。
5. PRoot/Linux companion/rootfs 未内置，供应链、许可证和实体机性能门禁仍关闭。
6. 未推正式更新通道、未创建正式 Release、未上传二进制到远程发布服务。

因此这些文件是“本地测试候选”，不是“正式签名发布完成”。
