# 麓鸣 2.4.5 r6：Windows Sandbox 重启检查点

> 日期：2026-08-03
>
> 分支：`codex/account-entitlement-matrix-20260729`
>
> 重启前 HEAD：`cd868ee0637412641c10cf35bf80c755fbc16d61`

## 已完成

- Windows 功能 `Containers-DisposableClientVM` 已从 `Disabled` 启用为 `Enabled`；系统返回 `RestartNeeded=true`。启用时使用 `-NoRestart`，没有提前中断构建与证据保存。
- 桌面 + 手机联合候选位于：
  `D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.5-lumiagent-6.66-20260803-r6`
- 精确桌面安装包：`麓鸣_2.4.5_x64-setup.exe`
  - 字节：`266559827`
  - SHA-256：`77F583899381D8620CEEBC730227B0504D1CC918EFB3C09411E6969BEB8869E9`
  - File/Product version：`2.4.5`
  - ProductName/Description：`麓鸣`
  - Authenticode：`NotSigned`，仅限本地测试候选
- r6 目录内其余 9 个 LumiAgent 6.66、Android 7、PRoot companion、instrumentation 和脱敏截图文件与 r5 逐文件 SHA-256 一致。
- Windows Sandbox 配置位于：
  `D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.5-windows-sandbox-20260803-r6\Luming-2.4.5-Acceptance.wsb`
- Sandbox 候选和验收脚本为只读映射，证据目录为可写映射，网络开启，USB 不透传；配置固定校验上述完整 SHA-256。生成器合同 `5/5` 通过。
- 生产 Z-Pay 代码与商户配置已迁移，但真实支付和主动查单闸门均关闭；生产 `payment_orders=0`，没有创建二维码、支付链接或扣款。

## 重启后必须按顺序继续

1. 在工作树只读核对 `git status --short`、当前分支、HEAD 与远程分支，确认没有覆盖他人改动。
2. 核对 `Containers-DisposableClientVM=Enabled` 且 `C:\Windows\System32\WindowsSandbox.exe` 存在。
3. 启动上述 `.wsb`。Windows Sandbox 不支持 USB 透传，因此桌面逐按钮流程在 Sandbox 内验收；真实 USB、热点/Wi-Fi、MediaProjection、Shizuku 和多机长稳仍使用实体机矩阵。
4. 依据 `apps\loom-platform\scripts\windows-sandbox-acceptance-checklist.md` 以小白路径逐按钮执行。任何实际点击“安装”或“运行”前，必须先进行 Computer Use 的动作时确认。
5. 证据只写入 r6 Sandbox 证据目录；记录精确安装包哈希、安装/升级结果、按钮路径、错误提示、截图与清理结果。
6. 支付验收继续保持无扣款：不得创建生产订单；在拿到权威 CNY 套餐映射以及商户沙箱/测试账号或供应方明确支持的零金额合同之前，不得打开 `LICENSE_ZPAY_ENABLED` 或 `LICENSE_ZPAY_QUERY_ENABLED`。
7. 完成 VM 证据后更新 `docs/release/2.4.5-build-evidence.md`，再提交并推送；不合并 PR、不更新正式下载通道、不创建正式 Release。

## 尚未满足的完成门禁

- r6 精确安装包在 Windows Sandbox 内的逐按钮验收尚未执行。
- 4 个 New API 套餐当前标记为 USD；缺少由产品方确认的 CNY 金额映射。
- 缺少 Z-Pay 沙箱/测试商户或供应方明确支持的零金额真实协议，因此不能声称扫码支付闭环已验收。
- 商业代码签名、实体 Android/USB/热点/Wi-Fi/Shizuku/MediaProjection、多机并发和长时间 soak 尚未完成。

## 重启后执行结果（2026-08-03）

- 系统已实际重启，Windows Sandbox 可正常创建和销毁；HNS、SharedAccess、vmcompute 与 winnat 服务恢复运行。
- r7 安装包完成全新安装和首次启动。随后在逐按钮检查中发现：沙盒无法访问外网时，账号能力探测失败会把“验证码登录”标签直接禁用。
- 通过失败测试复现后，已移除标签级 disabled；发送验证码仍保留后端能力门禁和明确错误提示。生产能力接口只读核对显示验证码登录与 SMTP 均已开放。
- 包含修复的 r8 安装包已在第二个全新 Windows Sandbox 中完成安装、内置 WebView2 离线运行时部署、首次启动，并实际点击进入“验证码登录”。
- r8 安装包 SHA-256：`9D429CCA0DB5735EB1066150FD43E17C9D99BBE59BBA3BE5F9176B004A83211E`；验收资产位于 `D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.5-windows-sandbox-20260803-r8`。
- 沙盒出站 TCP 443 仍为 `false`，所以本轮没有发送真实验证码；点击“发送验证码”只显示可操作的网络/邮箱能力提示，不产生外部动作。
- 当前只完成安装、首次启动和验证码入口的针对性 VM 验收。原检查清单中的 Agent 安装、手机绑定、共享模板、Skill 创建/复用、获客、飞书、支付状态恢复、退出及切换账号仍不能标记为全部通过。
- r8 继续检查时发现访客进入“智能体”被误报为账号切换。失败测试确认根因是空账号 ID 被直接视为切换；r9 只放行前后均明确 `loggedIn=false` 的只读访客，任何账号会话读取失败或账号变化仍 fail-closed。
- r9 安装包 SHA-256：`646FE2903D17CFFBEC777CA35497B96CDFB3F69162DD630D459C46B1CA56E671`；候选位于 `D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.5-lumiagent-6.66-20260803-r9`，Sandbox 资产位于 `D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.5-windows-sandbox-20260803-r9`。
- r9 Sandbox 客体已写出精确安装器清单和运行环境证据，但切入远程桌面后 Computer Use 不再返回可点击目标；没有猜测坐标或切换到不受控 UI 自动化。因此 r9 Sandbox 逐按钮结果仍未完成。
- 精确 r9 安装包在宿主隔离事务中完成全新安装和覆盖升级；两条均为 packaged Python/Node、protected 路由、82 项 Agent 能力、`guestAgentReadOnly=true`、授权接口 `200`、未授权矩阵/获客 `403`，覆盖升级 `upgradeDataPreserved=true`。
