# 麓鸣 2.4.4 授权后端部署门禁

> 审计日期：2026-08-02
>
> 后端候选源码提交：`37eb4818d1ac558ae25d9f59b18d9605ca735ff8`
>
> 状态：生产部署、单码绑定和 Desktop 无码刷新验收已完成；正式客户端发布仍未执行

## 0. 生产部署结果

用户于 2026-08-02 明确授权配置并部署生产 entitlement 后端，并允许从表格后 50 个候选中使用 1 个授权码验收。执行中未输出或持久化授权码明文、授权码哈希、账号身份、服务 Token 或私钥。

| 项目 | 生产结果 |
| --- | --- |
| 授权服务 | `openclaw-license.service` active；程序 SHA256 `316d9c912aef9f91cc7cbac24100a7c9f9f8f684ee0473c266695f1b806a9134`；SQLite `quick_check=ok` |
| 模型账户桥接 | `openclaw-newapi-bridge.service` active；程序 SHA256 `62c88424a1dd2cc44847a87d4f51eb033bab23489099b3c46a1a99ef471847d8`；SQLite `quick_check=ok` |
| systemd 沙箱 | 保留 `ProtectSystem=strict`；新增 `StateDirectory=openclaw-newapi-bridge`、`StateDirectoryMode=0700`；drop-in SHA256 `2eb4914d422d7c88fa590f37c31a6581e9e729c64b7a50a0e9bea59d637dd05c` |
| 公网合同 | `api.heang.top` 与 `api-cn.heang.top` 健康接口均 `200`；固定 Ed25519 信任锚均匹配；无认证 `/refresh` 均为 `401` |
| 单码绑定 | 只使用表格后 50 个候选中的 1 个；license 端绑定次数精确为 `1`；之后未再次提交授权码 |
| Desktop 恢复 | 仅调用无授权码 `/refresh`；签名 lease 和防回滚状态落盘；`matrix.devices` 授权和矩阵任务槽均通过 |

实际验收暴露并修复了五个部署/运行根因：服务启动的一次性健康探针竞态、边缘层拒绝默认 Python User-Agent、root 诊断误建绑定库导致所有权错误、`ProtectSystem=strict` 未声明可写状态目录，以及 entitlement 刷新错误耦合旧 launcher token 升级。所有修复均先有失败测试再实现。

## 1. 部署前线上故障边界

无凭据、无授权码的只读探针得到以下结果：

| 服务 | 探针 | 结果 |
| --- | --- | --- |
| 国内模型账户桥接 | `GET https://api-cn.heang.top/api/status` | `200` |
| 国内模型账户桥接 | `GET /api/openclaw/entitlements/public-key` | `404`，桥接应用自身返回 `success=false / error=not found` |
| 海外模型账户桥接 | `GET https://api.heang.top/api/status` | `200` |
| 海外模型账户桥接 | `GET /api/openclaw/entitlements/public-key` | `404`，桥接应用自身返回 `success=false / error=not found` |
| 授权服务 | `GET https://license.heang.top/health` | `200` |
| 授权服务 | `GET /api/service/account-entitlements/current` | `404` |

以上是部署前事实，证明当时反向代理和进程均可达，但生产进程加载的程序版本没有 entitlement 路由。生产部署后这些 `404` 已消失；公共密钥为 `200`，无认证刷新为 `401`。

### 1.1 生产资产只读发现

随后只用 SSH 执行 `systemctl show`、文件元数据、磁盘空间、哈希和路由字面量布尔检查；没有读取环境文件内容、私钥或数据库内容，也没有上传、重启或写入：

| 项目 | 授权服务 | 模型账户桥接 |
| --- | --- | --- |
| SSH 配置别名 | `huoshan` | `peiqianyun` |
| systemd unit | `openclaw-license.service` | `openclaw-newapi-bridge.service` |
| 运行状态 | `active` | `active` |
| 工作目录 | `/opt/openclaw-license` | `/opt/openclaw-newapi-bridge` |
| unit 文件 | `/etc/systemd/system/openclaw-license.service` | `/etc/systemd/system/openclaw-newapi-bridge.service` |
| 环境文件 | `/opt/openclaw-license/openclaw-license.env` | `/opt/openclaw-newapi-bridge/bridge.env` |
| Python | `3.10.12` | `3.10.12` |
| 部署盘可用空间 | `8,676,324 KiB` | `3,554,964 KiB` |
| 线上程序 SHA256 | `af3fd581…ac254` | `472f4fbc…b1889b` |
| 候选程序 SHA256 | `316d9c91…a9134` | `bf969f53…2baab2` |
| entitlement 路由字面量 | 不存在 | 不存在 |

授权服务的环境文件、SQLite 和私钥文件均存在且权限为 `600`；桥接环境文件也为 `600`。这里只记录存在性、权限和大小级别的部署前事实，不记录任何内容。线上程序哈希与候选不一致，且两边都明确缺少 entitlement 路由字面量，与公网 `404` 相互印证。

配置键存在性检查进一步确认：授权服务尚未配置 `LICENSE_ACCOUNT_REDEEM_SERVICE_TOKEN`；桥接服务尚未配置 entitlement 服务 Token/Base、持久化 bind DB、bind-ticket Secret、签名私钥来源和 key ID。检查只输出 `configured/missing`，没有读取值。桥接机器也不存在示例中的 `/opt/openclaw-license/private_key.b64`，因此不能照抄跨机器失效的默认路径。

## 2. 部署缺口与修复

旧 `apps/loom-platform/license_server/deploy.sh` 存在四个部署级缺口：

1. 只部署 `server.py`，没有部署其依赖的完整 `luming_license/` 模块树；
2. 配置发布中转令牌时会重写整个环境文件，可能擦除 entitlement 服务令牌和其他设置；
3. 健康检查只证明进程存活，没有证明 entitlement 路由已经加载；
4. 程序切换前后的失败路径没有统一覆盖环境和程序回滚。

提交 `3ce44e6` 已完成以下加固：

- 新增 `luming_license/deploy_env.py`，以原子写入方式只更新指定环境变量并保留其他配置；
- 部署完整 `server.py + luming_license/`，上传内容先编译，再在同一文件系统内暂存和切换；
- 部署前备份程序、环境和 SQLite；失败时回滚程序与环境；
- entitlement 服务令牌成为强制前置条件；无凭据路由探针必须返回 `401`，`404` 或 `503` 均判为部署失败；
- SQLite 只备份和执行 `quick_check`，失败时绝不自动降级数据库，避免用旧库覆盖部署期间的有效写入；
- Relay 状态上下文只比较稳定授权字段，且错误响应同时保留结构化对象和兼容字段。

提交 `991ad39` 又为已识别的桥接拓扑增加 `apps/loom-platform/server/deploy.sh`：

- 缺少任一 entitlement 配置时在 `systemctl stop` 前拒绝继续；
- 不 `source`、生成、复制或改写 `bridge.env` 和签名私钥；
- 候选先编译，并用生产环境配置加载候选、校验固定 key ID 与公钥信任锚；
- 备份旧程序和环境文件，使用同文件系统暂存与原子切换；
- 启动后同时验证 `/health` 与 `/api/openclaw/entitlements/public-key` 的 JSON 合同；
- 强制验证部署前后环境文件 SHA256 不变，失败时恢复旧程序并重新启动旧服务。

## 3. 当前测试证据

| 范围 | 结果 |
| --- | --- |
| 模型账户桥接目录完整回归 | `89/89` passed |
| 桥接部署静态合同 | `7/7` passed，含私有可写 StateDirectory 门禁 |
| 桥接部署隔离集成演练 | `4/4` passed，含延迟启动等待与公钥失败回滚 |
| 授权服务完整合同 | `206/206` passed |
| Desktop entitlement 客户端合同 | `32/32` passed |
| Desktop 模型账户管理合同 | `62/62` passed |
| 部署环境与脚本合同 | `6/6` passed |
| Relay 完整合同 | `30/30` passed |
| `deploy.sh` Bash 语法 | passed |
| 隔离目录成功部署演练 | `success_smoke=passed` |
| 强制 entitlement 探针失败回滚演练 | `rollback_smoke=passed` |

成功和回滚演练均只操作：

`D:\Axiangmu\LOOM-Workspace\temp\license-deploy-smoke-20260801-*`

演练使用本地伪造的 `systemctl`、`curl` 和隔离程序目录，没有连接或修改生产服务器。

## 4. 后端候选包

可审计目录：

`D:\Axiangmu\LOOM-Workspace\release-candidates\luming-entitlement-backend-37eb481-r6`

压缩包：

`D:\Axiangmu\LOOM-Workspace\release-candidates\luming-entitlement-backend-37eb481-r6.zip`

| 项目 | 结果 |
| --- | --- |
| ZIP 字节 | `127,550` |
| ZIP SHA256 | `7AD7759EA9534DF29A208301EB11EA6439F9FA7CC1C0254C22446078FFA4D0A9` |
| 实际载荷 | `47` 个文件，`SHA256SUMS.txt` 逐项全部匹配，且逐哈希匹配提交 `37eb481` 当前源文件 |
| ZIP 文件总数 | `49`（载荷 + README + 校验清单） |
| Python 源文件 | `41` |
| 守护式 Bash 部署脚本 | `2`，语法检查均通过；另含 `1` 个最小 systemd 状态目录 drop-in |
| 禁止内容 | `0` 个 `.pyc`、数据库、私钥、真实 `.env` 或已知用户私密标记 |
| 敏感配置示例 | `4` 个值，均为显式占位符，不是真实密钥 |

此前 `-r2` 至 `-r5` 候选均已被生产验收发现的后续修复取代。只允许使用提交 `37eb481`、带 `-r6` 的目录或 ZIP。

## 5. 生产执行顺序与验收门

以下生产顺序已在用户明确确认后执行，并作为后续回滚/再部署的固定门禁：

1. 重新核对已只读发现的两个真实 unit、工作目录、环境文件、反向代理路由和 SQLite 路径；
2. 生成脱敏清单并确认备份空间；
3. 通过安全通道生成一个专用服务间 Token，在授权服务和桥接环境中配置同一值；配置持久化 bind DB、独立 bind-ticket Secret 和与客户端信任锚匹配的既有签名私钥来源；不在命令行、日志或候选包中出现值；
4. 先部署授权服务，要求健康、SQLite、进程和 entitlement `401` 路由探针全部通过；
5. 再用守护脚本部署桥接服务，要求本地和公网公共密钥路由 `200` 且返回可解析公钥；
6. 使用现有模型账号会话执行不含授权码的 current/refresh 验证；
7. 只从用户表格后 50 个候选中使用先前安全选定的 1 个授权码；明文只驻留进程内；
8. 验证账号绑定、Desktop 刷新、矩阵解锁和同码重复请求幂等；
9. 记录状态码、时间、匿名状态和授权版本，不记录授权码、Token、私钥或账号明文。

任一步失败都停止后续动作。程序或环境失败可按备份回滚；数据库不得自动回滚。正式发布、更新清单、支付、评论、私信和其他对外动作均不在本门禁的默认授权范围内。

## 6. 当前结论

生产 entitlement 后端已完成配置、守护部署、单码绑定和 Desktop 无码刷新闭环，线上授权链路现已修复。该结论不等同于 Desktop/APK 正式发布完成；商业签名、实体机矩阵以及正式更新通道仍受各自发布门禁约束。
