# 麓鸣 2.4.2 / LumiAgent 6.64 本地候选证据

> 构建日期：2026-08-01
>
> 产品源码提交：`41272f05d1fc079868118a5bebaa69d09618def2`
>
> 分支：`codex/account-entitlement-matrix-20260729`
>
> 范围：本地测试候选；未推更新通道、未创建正式 Release、未对外发布

## 1. 产物

候选目录：

`D:\Axiangmu\LOOM-Workspace\release-candidates\luming-2.4.2-41272f05d1fc`

| 产物 | 字节 | SHA256 | 版本/兼容性 | 签名状态 |
| --- | ---: | --- | --- | --- |
| `麓鸣_2.4.2_x64-setup.exe` | 266,510,496 | `E79382485C7F7CD33C68B3DAF58E55447C3F3626F4AFD73E3AD7612AC2811C4E` | File/Product `2.4.2`，ProductName/Description `麓鸣` | Authenticode `NotSigned`，仅本地测试 |
| `AgentPhone_v6.64-stability_20260801_174956.apk` | 52,091,108 | `FCBC65669F7237D6BC24DF1B90A4D419A316D515472B0535CADC8CAC1ABA16B2` | versionCode `933`，versionName `6.64-stability`，minSdk `28` | APK v2 调试签名 |
| `AgentPhone_v6.64-stability-android7_20260801_175051.apk` | 19,645,761 | `15BBA549BFEDBA8D5DFDDE3189152CC23C4E76B171F6243A7594F611006413AA` | versionCode `933`，versionName `6.64-stability-android7`，minSdk `24` | APK v2 调试签名 |

两份调试 APK 的证书 SHA-256 均为：

`F6194A531BDD82F020080D21567E88864A1D8AC4BC27904FDF5DF46151426B6D`

该证书只用于本地调试候选，不等同于 `release/trusted-signing-cert.sha256` 约束的正式发布证书。真实执行 `:app:assembleRelease` 会因 `KEYSTORE_FILE`、`KEYSTORE_PASSWORD`、`KEY_ALIAS`、`KEY_PASSWORD` 未配置而失败关闭；没有借用其他项目的未知 `.jks`。

## 2. 自动化结果

| 层 | 命令/门禁 | 结果 |
| --- | --- | --- |
| Frontend | `npm run test:platform-contracts` | `221/221` passed |
| Node/CLI | `npm run test:node-contracts` | `100/100` passed |
| Python | `python -m pytest python/tests -q` | `2055` passed，`367` subtests passed |
| Shared contracts | `python packages/contracts/validate_contracts.py` | `11/11` Draft 2020-12 schemas + fixtures |
| Android default | `:app:testDebugUnitTest` | `663/663`，0 failed/errors/skipped |
| Android 7 | `-Pandroid7Compat=true :app:testDebugUnitTest` | `663/663`，0 failed/errors/skipped |
| Rust | `cargo check --locked` | passed |
| Rust tests | `cargo test --locked` | `30/30` passed |
| Production frontend | `npm run build` | passed，Vite `2133` modules transformed |
| Protected release | `stage:protected` + `verify:protected` | Python `92` compiled + `6` loaders；scripts `23` copied + `25` obfuscated；verified |
| Desktop NSIS | `npm run package:protected:nsis` | passed，生成一个 x64 bundle；一次性目录静默安装/卸载均返回 `0`，安装目录被卸载器完整清除 |
| Mobile runtime benchmark | host harness smoke | passed；Linux adapter `not_measured_adapter_missing`，性能声明禁止 |

首轮全量回归曾发现 3 个 Python 失败和 1 个 Android 源码契约失败；定向修复后重新执行完整 Python 与 Android 双变体，以上表格记录的是修复后的完整结果。

## 3. 版本与品牌核对

- Desktop `package.json`、`package-lock.json`、`tauri.conf.json`、`Cargo.toml`、`Cargo.lock` 均为 `2.4.2`。
- Tauri `productName`、窗口标题、Windows 文件 ProductName/Description 均为“麓鸣”。
- 完整 Logo 已进入生产前端资源；启动界面原动画未改。
- LumiAgent 两个变体均为 versionCode `933`，并通过真实 APK manifest 检查。

## 4. 构建环境

- 本次重建前 C 盘空闲 `3.88 GB`、D 盘空闲 `71.97 GB`；产物与虚拟机校验后约为 C `3.86 GB`、D `71.64 GB`。
- Android SDK：`D:\android-sdk-windows\android-sdk-windows`，build-tools `36.0.0`。
- Rust/Tauri 使用本机 Visual Studio 2022 Build Tools 的 x64 MSVC 环境。
- 未删除用户文件、未清理未知缓存、未覆盖旧 `2.4.0` 安装包。

## 5. Android 虚拟机补充验证

2026-08-01 使用本机官方 Android Emulator + WHPX，在 D 盘创建两个互相隔离的 AVD；未清除旧 `APKClaw_API36` 数据，也未把虚拟机结果计作实体机通过：

| AVD | 系统镜像 | 候选安装 | UI/状态机 | Instrumentation |
| --- | --- | --- | --- | --- |
| `LumiAgent_API36_242` | Android 16 / API 36，Google APIs x86_64 | default APK SHA256 匹配；versionCode `933`、versionName `6.64-stability` | 冷启动无崩溃；设置首屏顺序、发布中转入口移除、USB 六位码、LAN 模式均可见；`9527` 实际监听连续开/关 `50/50`，无重复监听；开/关状态在进程重启后均按用户最后选择恢复 | `6/6`，0 failed/errors/skipped |
| `LumiAgent_API24_242` | Android 7.0 / API 24，Google APIs x86_64 | android7 APK SHA256 匹配；versionCode `933`、versionName `6.64-stability-android7`、minSdk `24` | 冷启动无崩溃；设置首屏和 USB 六位码真实渲染；`9527` 实际监听连续开/关 `50/50`，无重复监听；关闭状态重启后保持关闭，开启状态由旧系统前台服务按设计恢复 | `6/6`，0 failed/errors/skipped |

API 24 首轮 instrumentation 的 `refreshing_usb_pairing_revokes_the_previous_payload` 因 1920 高度屏幕中刷新按钮位于视口外而失败；生产功能可滚动使用。测试已改为在普通 `ScrollView` 中先 `scrollTo()` 再 `click()`，随后 API 24 与 API 36 两端完整重跑均为 `6/6`。

另在 API 24 上真实执行 ADB forward：

1. 手机服务启动后建立 `emulator-5554 tcp:19527 tcp:9527`。
2. Windows 主机请求 `http://127.0.0.1:19527/` 返回 HTTP `200`，正文长度 `15,717`，能够识别 Lumi/LOOM 页面。
3. 从手机设置页关闭服务后，`9527` 监听消失，同一主机请求返回连接失败；随后删除 forward 规则。

虚拟机证据目录：

`D:\AndroidAVD\evidence\lumiagent-2.4.2`

其中 `api36-instrumentation.xml` 与 `api24-instrumentation.xml` 分别保存两端测试 XML；目录还包含启动、设置页与配对页截图。截图中的一次性配对码和载荷仅属于隔离 AVD，未写入仓库日志或发布产物。

历史衔接提交 `41272f0` 使用双亲 `ours` 合并接回远程已有的等价授权提交，文件树与其第一父提交 `b8e0de0` 完全一致。2026-08-01 重新打包后，又把上表列出的两份当前 APK 原样安装到 API 36/API 24 虚拟机：版本、`minSdk` 与进程启动状态均正确，两端 instrumentation 再次完整运行并各为 `6/6`。虚拟机随后已关闭，ADB 无遗留设备。

虚拟机不能证明物理手机热点、真实 USB 枚举/拔插、OEM MediaProjection、Shizuku Binder、温升/功耗或多台实体设备长稳；这些门禁继续单独保留。

## 6. 尚未通过的正式发布门禁

1. 已完成 Android 7/API 24 和 Android 16/API 36 虚拟机补充验证，但仍无实体机；无法完成物理 Android 7/10/14+、热点/Wi-Fi/USB、USB+Wi-Fi、2 台并发、10 台两小时 soak、30 分钟视频与 20 次拔插。
2. 无正式 Android release keystore/密码/别名；只能交付调试签名 APK。
3. 无 Windows 商业代码签名证书；NSIS 候选为 `NotSigned`。
4. Shizuku 的 ADB/无线调试、binder 中断、撤权与重启恢复尚缺实体机证据。
5. 2.4.2 未内置 PRoot/Linux companion/rootfs；供应链、许可证和 Android 实机性能门禁未开启。
6. 产物源码提交 `41272f0` 已与同名远程同步（ahead `0` / behind `0`）；未强推、未创建 PR、未推更新通道，也未创建正式 Release。

因此这些文件是“本地测试候选”，不是“正式签名发布完成”。
