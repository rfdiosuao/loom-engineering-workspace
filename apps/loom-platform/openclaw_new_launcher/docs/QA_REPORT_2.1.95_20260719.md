# LOOM 2.1.95 Agent Harness 专项验收

## 新增缺陷

| # | 严重度 | 缺陷 | 修复结果 |
|---|---|---|---|
| 62 | P0 | 模型漏掉 `loom.phone.publish` 必填标题时，运行在审批前立即失败 | 校验错误回送模型一次，不执行工具、不创建审批；补齐后才生成审批 |
| 63 | P0 | 模型收到校验错误后可能只返回文字，运行被错误标为完成 | 校验自纠轮锁定原工具，并使用兼容供应商的内部纠错消息；失败不再被文字收尾掩盖 |

2.1.93 与 2.1.94 已记录 61 项缺陷，本轮累计记录 63 项真实缺陷。

## 真实模型边界验证

- 使用本机已登录的麓鸣模型账户和真实网关验证，未输出或持久化账户密钥。
- 第一轮由测试传输层固定生成缺少 `title` 的发布调用。
- Harness 记录 1 次内部 `tool.input_rejected`，聊天区可见 `tool.failed` 为 0。
- 第二轮请求继续锁定 `loom.phone.publish`，消息角色为 `system/user/user`，不再使用供应商不兼容的失败工具历史。
- 真实模型补齐 `title`、`body`、`deviceId`、`mediaPaths` 和 `draftOnly=true`。
- 最终运行状态为 `waiting_approval`，审批数量为 1，手机执行器未运行。

## 当前外部前置条件

- Android 模拟器网络、DNS 和系统权限可用，抖音已能进入手机号登录页。
- 抖音账户仍未登录，真实草稿需要人工输入手机号和验证码。
- 麓鸣模型账户凭据不等于抖音账户凭据，不会跨系统复用。

## 回归结果

| 测试层 | 结果 |
|---|---:|
| Python | 1206 passed |
| 前端合同 | 116 passed |
| Node 手机、媒体与发布合同 | 59 passed |
| Rust | 19 passed |
| Playwright 桌面 E2E | 213 passed, 6 skipped |

- E2E 使用单工作器完成 960×640、1200×800、1440×900 全量回归，最终 0 失败。
- Logo 断言同时验证元素可见、图片完成解码且 `naturalWidth > 0`。
- 启动器内邮箱注册入口保持删除，网页注册打开失败也会显示可操作错误。
- `git diff --check`、Python 编译检查、前端生产构建与受保护资源校验均通过。

## 安装包验收

- 全新安装：打包 FastAPI、Python、Node、受保护路由和麓鸣原生 Agent 正常。
- 升级安装：旧托管文件可覆盖，用户数据保留。
- Agent 能力目录：81 项。
- 文件：`Luming AI Matrix Acquisition Workbench_2.1.95_x64-setup.exe`
- 大小：257,707,753 字节（245.77 MiB）。
- SHA256：`6E0A56D70D47D1080E1249A913125009FC74A2073388F80E28CBC8E444C30949`。
