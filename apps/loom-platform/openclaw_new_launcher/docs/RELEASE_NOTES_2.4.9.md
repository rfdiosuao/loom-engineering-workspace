# 麓鸣 2.4.9 更新日志

- 修复自动更新交接进程提前正常退出后无法完成升级的问题，并增加编码命令兼容重试和启动日志。
- 修复安全快照状态下商业矩阵授权码输入框不可编辑、无法激活的问题。
- 将 New API 内部 quota 按 `500000 quota = ¥1.00` 换算为人民币余额和累计消费。
- 保持 LumiAgent `6.67-stability`、Android 7 兼容版和手机 versionCode `936` 不变。
