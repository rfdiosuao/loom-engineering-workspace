# LOOM 2.1.107 更新说明

## 矩阵截图与控制

- 将矩阵截图请求预算由 15 秒提升到 40 秒，与后端任务预算保持一致，避免手机仍在正常返回画面时被提前判定超时。
- 截图失败后采用渐进退避重试，避免连续刷新占满 APKClaw 通道并拖慢任务下发。
- 已取得上一帧画面时，临时截图刷新失败不再禁用返回、主页、文字输入等人工控制。
- 画面刷新较慢时继续显示上一帧，并提供明确提示和手动重试入口。

## 稳定性

- 手机连接与矩阵工作台继续复用同一套手机配置、凭据和控制协议。
- 增加慢截图、矩阵超时参数、失败退避和控制可用性的回归测试。
