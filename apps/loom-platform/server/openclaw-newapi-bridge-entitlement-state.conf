[Service]
# Preserve ProtectSystem=strict while allowing the bridge to persist signed
# account entitlement grants under /var/lib/openclaw-newapi-bridge.
StateDirectory=openclaw-newapi-bridge
StateDirectoryMode=0700
